# Exclude vendored third-party libraries from GitHub language statistics
C++_Server/header_libs/** linguist-vendored

# Classify React files as JSX rather than grouping them with JavaScript
*.jsx linguist-language=JSX
